# load nvm
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm

# prompt
source ~/.zsh/prompt

# aliases
source ~/.zsh/aliases

# exports
source ~/.zsh/exports

# completion
source ~/.zsh/completion

